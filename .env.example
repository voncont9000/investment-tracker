TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
DB_PATH=data/tracker.db
POLL_INTERVAL_MINUTES=15
WATCHLIST_DROP_THRESHOLD=-0.10
HOLDING_GAIN_THRESHOLD=0.10
TRAILING_WINDOW_HOURS=12

# Needed for the "Analyse COMPANY" command. Leave unset and every other
# command still works — only Analyse replies with a setup message.
ANTHROPIC_API_KEY=

# --- Future features (see plan doc "Extensibility") — not needed yet ---
# QUIVER_QUANT_API_KEY=   # only needed once the daily recommendation agent is built
# DAILY_DIGEST_TIME=07:00 # only needed once the daily recommendation agent is built
# TIMEZONE=               # e.g. Europe/Berlin; only needed once the daily recommendation agent is built
